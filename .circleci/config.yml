version: 2.1

orbs:
  node: circleci/node@4.7.0

jobs:
  npm_publish:
    executor: node/default
    steps:
      - checkout # checkout source code to working directory
      - run:
          command: |
            echo "//$NPM_REGISTRY:8080/:_authToken=$NPM_PUBLISH_TOKEN" 
            echo "//$NPM_REGISTRY:8080/:_authToken=$NPM_PUBLISH_TOKEN" > ~/.npmrc
            npm publish ./package

workflows:
  main:
    jobs:
      # - node/test
      - npm_publish #:
        # requires:
        #   - node/test
